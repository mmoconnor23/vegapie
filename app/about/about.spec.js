'use strict';

describe('mySite.about module', function() {

  beforeEach(module('mySite.about'));

  describe('about controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var aboutCtrl = $controller('aboutCtrl');
      expect(aboutCtrl).toBeDefined();
    }));

  });
});